# MDskill 安装使用指南

<div align="center">

**专业 Markdown 编辑器 for Mac**

v1.6.0 | 支持文档搜索 | 50+ 精选主题 | 一键复制到公众号

</div>

---

## 📦 安装步骤

### 1️⃣ 下载安装包
- 文件名：`MDskill-1.6.0-arm64.dmg`
- 适用于：Apple Silicon (M1/M2/M3/M4) Mac
- 文件大小：约 101 MB

### 2️⃣ 安装应用
1. 双击打开 `MDskill-1.6.0-arm64.dmg`
2. 将 **MDskill.app** 拖动到 **Applications** 文件夹
3. 如提示替换旧版本，点击"替换"

### 3️⃣ 首次启动处理

#### ⚠️ 如果提示"应用已损坏"或"无法打开"

**不用担心！这不是病毒或恶意软件！**

这个提示出现是因为：
- MDskill 没有上架 Mac App Store
- 没有向 Apple 购买开发者签名证书（每年需支付 $99）
- 这是 macOS 对所有非 App Store 应用的标准安全提示

**MDskill 完全安全**，代码开源透明，无任何恶意行为。

#### 解决方法（任选一种）

**方法一：系统设置中允许（推荐）**
1. 尝试打开 MDskill，看到提示后点击"取消"
2. 打开"系统偏好设置" > "安全性与隐私"
3. 在"通用"标签下，会看到 MDskill 被拦截的提示
4. 点击"仍要打开"按钮
5. 再次确认"打开"

**方法二：右键打开**
1. 在 Applications 文件夹中找到 MDskill
2. **按住 Control 键点击**（或右键点击）MDskill 图标
3. 选择"打开"
4. 在弹出的对话框中点击"打开"

**方法三：终端命令（最快）**
1. 打开"终端"应用（在 Applications > 实用工具 中）
2. 复制粘贴以下命令，按回车：
   ```bash
   xattr -cr /Applications/MDskill.app
   ```
3. 直接打开 MDskill，不会再有任何提示

---

## ⭐️ 完整功能介绍

### 📝 基础编辑功能

#### 1. 实时预览
- **左右分栏**：左侧编辑，右侧实时预览
- **所见即所得**：输入即可看到渲染效果
- **同步滚动**：编辑器和预览区同步滚动

#### 2. Markdown 全语法支持
- ✅ 标题（# ~ ######）
- ✅ 粗体、斜体、删除线
- ✅ 无序列表、有序列表、任务列表
- ✅ 链接和图片
- ✅ 代码块和行内代码
- ✅ 表格
- ✅ 引用块
- ✅ 分割线
- ✅ GitHub Flavored Markdown (GFM)

#### 3. 文件操作
- **新建窗口** (`Cmd+N`) - 创建新的编辑窗口
- **打开文件** (`Cmd+O`) - 支持 .md, .markdown, .txt 格式
- **保存文件** (`Cmd+S`) - 保存到本地
- **另存为** (`Cmd+Shift+S`) - 保存到指定位置
- **自动保存** - 编辑内容自动保存，防止丢失
- **拖拽打开** - 拖动文件到应用图标直接打开

### 🔍 文档搜索功能（新）

#### 1. 关键词搜索
- **打开方式**：
  - 点击工具栏 🔍 搜索按钮
  - 快捷键 `Cmd+F`
- **功能特性**：
  - 实时搜索匹配
  - 显示匹配数量（如：3/15）
  - 上一个/下一个匹配项快速导航
  - 自动滚动并高亮选中
  - 智能填充选中文本

#### 2. 正则表达式搜索
- **启用方式**：点击搜索框的 `.*` 按钮
- **使用场景**：
  - 匹配特定模式：`\b\w{5}\b`（5个字母的单词）
  - 查找所有二级标题：`^\#\# `
  - 匹配邮箱：`\S+@\S+\.\S+`
  - 匹配数字：`\d+`

#### 3. 搜索选项
- **大小写敏感** (`Aa` 按钮)
  - 开启：区分大小写
  - 关闭：不区分大小写（默认）
- **搜索结果列表**
  - 显示所有匹配项的行号
  - 点击结果直接跳转

### 📋 大纲导航功能（新）

#### 1. 自动大纲生成
- **打开方式**：
  - 点击工具栏 📋 大纲按钮
  - 快捷键 `Cmd+Shift+O`
- **功能特性**：
  - 自动提取所有标题（# ~ ######）
  - 按层级缩进显示
  - 一键跳转到任意章节
  - 当前位置自动高亮

#### 2. 适用场景
- 长文档快速浏览结构
- 书籍章节编辑
- 技术文档章节跳转
- 检查文档层级结构

### 💻 代码高亮

#### 1. 支持语言（180+）
- **前端**：JavaScript, TypeScript, HTML, CSS, Vue, React
- **后端**：Python, Java, Go, PHP, Ruby, C++, C#, Rust
- **数据库**：SQL, MongoDB, Redis
- **运维**：Shell, Bash, Docker, YAML
- **其他**：Markdown, JSON, XML, etc.

#### 2. 代码块功能
- 自动语法高亮
- 保持代码缩进
- 支持行号显示

### 🔢 数学公式支持

#### 1. 行内公式
```markdown
质能方程：$E = mc^2$
勾股定理：$a^2 + b^2 = c^2$
```

#### 2. 块级公式
```markdown
$$
\int_{-\infty}^{\infty} e^{-x^2} dx = \sqrt{\pi}
$$
```

#### 3. 支持的符号
- 希腊字母：`\alpha`, `\beta`, `\gamma`
- 积分：`\int`, `\sum`, `\prod`
- 分数：`\frac{a}{b}`
- 根号：`\sqrt{x}`
- 矩阵、向量、集合等

### 🎨 主题系统

#### 1. 50+ 精选主题
**技术文档类**：
- GitHub Dark / Light
- VS Code Dark / Light
- Monokai / Dracula
- One Dark / Atom

**商务风格类**：
- 简洁商务
- 专业报告
- 正式文档

**学术论文类**：
- 经典学术
- 论文格式
- 引用风格

**创意设计类**：
- 现代时尚
- 艺术风格
- 杂志排版

**公众号专用**：
- 微信风格
- 优雅排版
- 清新简约

#### 2. 主题切换
- **方式一**：点击工具栏主题图标 🎨
- **方式二**：使用主题选择下拉菜单
- **实时预览**：切换主题立即看到效果
- **自动保存**：记住上次使用的主题

### 📤 复制导出功能

#### 1. 复制到微信公众号 (`Cmd+Shift+W`)
- **一键复制**：保持所有格式
- **直接粘贴**：到公众号编辑器无需调整
- **完美兼容**：代码块、表格、公式全支持
- **推荐主题**：微信风格主题效果最佳

#### 2. 复制到博客 (`Cmd+Shift+C`)
- 适配各大博客平台
- 保持样式和格式
- 支持代码高亮

#### 3. 复制 HTML 源码 (`Cmd+Shift+H`)
- 获取完整 HTML 代码
- 包含样式定义
- 可独立使用

#### 4. 导出 PDF (`Cmd+E`)
- 高质量 PDF 导出
- 保持主题样式
- 适合打印和分享
- 所见即所得

### ✏️ 文本格式化

#### 1. 快速格式化按钮
- **加粗** (`Cmd+B`) - 选中文本一键加粗
- **斜体** (`Cmd+I`) - 选中文本一键斜体
- **代码** - 插入行内代码
- **链接** (`Cmd+K`) - 快速插入链接

#### 2. 工具栏快捷按钮
- 直接点击工具栏按钮
- 自动添加对应的 Markdown 语法
- 选中文本后点击，自动包裹

### 🔄 AI 格式化（可选功能）

#### 1. AI 智能格式化
- 将普通文本转换为规范的 Markdown
- 自动识别标题、列表、代码
- 优化排版和格式

#### 2. 使用方法
- 点击工具栏 "MD" 按钮
- 或使用快捷键 `Cmd+Shift+F`
- 首次使用需配置 AI API（可选）

---

## ⌨️ 完整快捷键列表

### 文件操作
| 快捷键 | 功能 |
|--------|------|
| `Cmd+N` | 新建窗口 |
| `Cmd+O` | 打开文件 |
| `Cmd+S` | 保存文件 |
| `Cmd+Shift+S` | 另存为 |

### 搜索导航
| 快捷键 | 功能 |
|--------|------|
| `Cmd+F` | 🔍 打开搜索面板 |
| `Cmd+Shift+O` | 📋 打开大纲导航 |
| `Enter` | 搜索：下一个匹配项 |
| `Shift+Enter` | 搜索：上一个匹配项 |
| `ESC` | 关闭搜索/大纲面板 |

### 文本编辑
| 快捷键 | 功能 |
|--------|------|
| `Cmd+B` | 加粗文本 |
| `Cmd+I` | 斜体文本 |
| `Cmd+K` | 插入链接 |

### 视图操作
| 快捷键 | 功能 |
|--------|------|
| `Cmd+P` | 切换预览面板 |

### 导出功能
| 快捷键 | 功能 |
|--------|------|
| `Cmd+E` | 导出 PDF |
| `Cmd+Shift+W` | 复制到微信公众号 |
| `Cmd+Shift+C` | 复制到博客 |
| `Cmd+Shift+H` | 复制 HTML 源码 |

### AI 功能（可选）
| 快捷键 | 功能 |
|--------|------|
| `Cmd+Shift+F` | AI 格式化 Markdown |

---

## 🚀 快速上手

### 第一次使用

#### 1. 新建文档
1. 打开 MDskill
2. 点击工具栏 **"新建窗口"** 按钮（或 `Cmd+N`）
3. 开始输入 Markdown 内容

#### 2. 打开已有文档
1. 点击工具栏 **"打开文件"** 按钮（或 `Cmd+O`）
2. 选择 .md/.markdown/.txt 文件
3. 或直接拖动文件到 MDskill 图标

#### 3. 编辑和预览
1. 在左侧编辑器输入内容
2. 右侧实时预览效果
3. 按 `Cmd+S` 保存

### 使用搜索功能

#### 场景 1：快速查找关键词
1. 按 `Cmd+F` 打开搜索
2. 输入关键词（如"性能优化"）
3. 看到匹配计数（如 3/15）
4. 按 `Enter` 跳转到下一个匹配项
5. 按 `Shift+Enter` 返回上一个

#### 场景 2：使用正则表达式
1. 按 `Cmd+F` 打开搜索
2. 点击 `.*` 按钮启用正则模式
3. 输入正则表达式：
   - `^\#\# ` - 查找所有二级标题
   - `\b\w{5}\b` - 查找5个字母的单词
   - `https?://\S+` - 查找所有链接
4. 浏览匹配结果

#### 场景 3：智能填充搜索
1. 在编辑器中选中要搜索的文本
2. 按 `Cmd+F` 打开搜索
3. 搜索框自动填充选中的内容
4. 立即显示所有匹配项

### 使用大纲导航

#### 场景 1：浏览文档结构
1. 按 `Cmd+Shift+O` 打开大纲
2. 查看所有标题的层级结构
3. 快速了解文档组织方式

#### 场景 2：快速跳转章节
1. 打开大纲面板
2. 点击任意标题
3. 编辑器自动跳转到对应位置
4. 当前章节在大纲中高亮显示

#### 场景 3：长文档编辑
1. 编辑书籍、论文等长文档
2. 使用大纲快速切换章节
3. 不用频繁滚动查找内容

---

## 💡 实用技巧

### 搜索技巧

#### 1. 智能填充
- 选中想搜索的文本
- 按 `Cmd+F`
- 自动填充到搜索框，立即看到所有匹配项

#### 2. 正则表达式示例
```
# 查找所有标题
^#+ 

# 查找所有链接
\[.*?\]\(.*?\)

# 查找所有邮箱
\S+@\S+\.\S+

# 查找所有数字
\d+

# 查找5个字母的单词
\b\w{5}\b

# 查找所有代码块
```.*?```
```

#### 3. 大小写处理
- 默认不区分大小写（查找"hello"会匹配"Hello", "HELLO"）
- 点击 `Aa` 按钮开启大小写敏感
- 适合区分专有名词

### 大纲技巧

#### 1. 检查文档结构
- 打开大纲查看层级是否合理
- 确保没有跳级（如一级标题后直接三级）
- 检查标题命名是否清晰

#### 2. 长文档导航
- 编辑时保持大纲面板打开
- 当前编辑位置自动高亮
- 需要跳转时直接点击标题

#### 3. 写作辅助
- 先列出大纲结构
- 使用标题占位
- 逐个章节填充内容

### 主题选择技巧

#### 1. 根据用途选择
- **技术文档**：GitHub Dark, VS Code
- **公众号文章**：微信风格，清新简约
- **博客文章**：简洁商务，现代时尚
- **学术论文**：经典学术，论文格式
- **演示汇报**：专业报告，正式文档

#### 2. 预览技巧
- 切换主题后立即看到效果
- 多试几个找到最合适的
- 保存后下次自动使用

### 复制导出技巧

#### 1. 公众号文章
1. 选择"微信风格"主题
2. 预览效果
3. 按 `Cmd+Shift+W` 复制
4. 在公众号编辑器中 `Cmd+V` 粘贴
5. **注意**：使用 Safari 或 Chrome 浏览器

#### 2. PDF 导出
1. 选择喜欢的主题
2. 预览效果（所见即所得）
3. 按 `Cmd+E` 导出
4. 选择保存位置

#### 3. HTML 导出
1. 按 `Cmd+Shift+H` 复制 HTML 源码
2. 包含完整样式
3. 可以独立使用或嵌入网站

---

## 🎯 典型使用场景

### 场景 1：写技术博客

**步骤**：
1. 打开 MDskill，新建文件
2. 切换到 **GitHub Dark** 主题
3. 编写文章，添加代码块
4. 使用 `Cmd+F` 检查术语是否统一
5. 按 `Cmd+Shift+C` 复制到博客

**技巧**：
- 使用代码块语法高亮
- 添加标题便于大纲导航
- 使用表格整理对比信息

### 场景 2：编辑公众号文章

**步骤**：
1. 切换到 **微信风格** 主题
2. 编写文章内容
3. 添加图片和引用
4. 预览最终效果
5. 按 `Cmd+Shift+W` 一键复制
6. 粘贴到公众号编辑器

**技巧**：
- 使用引用块突出重点
- 适当使用加粗和斜体
- 控制段落长度便于阅读

### 场景 3：编写技术文档

**步骤**：
1. 规划文档结构，添加标题大纲
2. 使用 `Cmd+Shift+O` 查看大纲
3. 逐个章节编写内容
4. 添加代码示例和表格
5. 使用搜索检查专业术语
6. 导出 PDF 或 HTML

**技巧**：
- 保持标题层级清晰
- 代码块注明语言类型
- 使用表格对比参数
- 添加数学公式说明算法

### 场景 4：编辑长文档（书籍/论文）

**步骤**：
1. 打开长文档
2. 按 `Cmd+Shift+O` 打开大纲
3. 点击大纲跳转到要编辑的章节
4. 编辑完成后通过大纲跳转到下一章节
5. 使用搜索功能检查全文

**技巧**：
- 大纲面板保持打开
- 使用搜索检查术语一致性
- 定期保存（自动保存已开启）
- 使用任务列表跟踪进度

### 场景 5：整理学习笔记

**步骤**：
1. 按主题创建标题
2. 记录关键知识点
3. 添加代码示例
4. 使用任务列表标记重点
5. 使用标签便于搜索

**技巧**：
- 使用二级、三级标题分类
- 重要内容使用引用块
- 代码片段添加注释
- 使用搜索快速找到内容

---

## ❓ 常见问题

### 安装相关

#### Q1: 提示"应用已损坏"怎么办？
**A:** 这是正常的系统安全提示，不是病毒！

**原因**：MDskill 没有上架 App Store，也没有购买 Apple 的开发者签名（$99/年）

**解决方法**（任选一种）：
1. **最快方法**：终端执行 `xattr -cr /Applications/MDskill.app`
2. 系统设置中点击"仍要打开"
3. 右键点击应用，选择"打开"

**保证**：MDskill 完全安全，无任何恶意代码

#### Q2: 应用无法启动？
**A:** 
1. 确认已将应用拖到 Applications 文件夹
2. 使用上述方法解除系统限制
3. 如仍有问题，联系微信：**AIPMAndy**

### 使用相关

#### Q3: 搜索功能在哪里？
**A:** 
- 点击工具栏的 🔍 按钮
- 或按 `Cmd+F` 快捷键

#### Q4: 如何使用正则表达式搜索？
**A:** 
1. 打开搜索面板（`Cmd+F`）
2. 点击 `.*` 按钮启用正则模式
3. 输入正则表达式（参考上面的示例）

#### Q5: 大纲在哪里打开？
**A:** 
- 点击工具栏的 📋 按钮
- 或按 `Cmd+Shift+O` 快捷键

#### Q6: 如何切换主题？
**A:** 点击工具栏的主题图标 🎨，或使用主题选择下拉菜单

#### Q7: 复制到微信公众号格式乱了？
**A:** 
1. 使用 **Safari** 或 **Chrome** 浏览器打开公众号后台
2. 选择"微信风格"主题
3. 按 `Cmd+Shift+W` 复制
4. 在编辑器中 `Cmd+V` 粘贴
5. 不要使用微信开发者工具

#### Q8: 支持哪些文件格式？
**A:** 
- .md（Markdown）
- .markdown
- .txt（纯文本）

#### Q9: 文件保存在哪里？
**A:** 
- 自动保存在原文件位置
- 新建文件首次保存时选择位置
- 支持"另存为"到其他位置

#### Q10: 支持图片吗？
**A:** 
- 支持本地图片：`![描述](./image.png)`
- 支持网络图片：`![描述](https://example.com/image.jpg)`
- 拖动图片到编辑器会自动生成语法

### 功能相关

#### Q11: 数学公式怎么写？
**A:** 
- 行内公式：`$E = mc^2$`
- 块级公式：
  ```
  $$
  公式内容
  $$
  ```
- 使用 LaTeX 语法

#### Q12: 代码高亮支持哪些语言？
**A:** 支持 180+ 语言，包括：
- JavaScript, Python, Java, C++, Go, Rust
- HTML, CSS, SQL, Shell
- 以及更多（几乎涵盖所有主流语言）

#### Q13: PDF 导出在哪里？
**A:** 
- 按 `Cmd+E` 导出 PDF
- 或点击工具栏的导出按钮
- 选择保存位置

#### Q14: 可以导出 HTML 吗？
**A:** 
- 按 `Cmd+Shift+H` 复制 HTML 源码
- 包含完整的样式定义
- 可以独立使用

#### Q15: AI 格式化是什么？
**A:** 
- 可选功能，需配置 AI API
- 将普通文本智能转换为 Markdown
- 自动识别标题、列表、代码等
- 首次使用会提示配置

---

## 🆕 更新说明（v1.6.0 - 2026/06/02）

### ✨ 新增功能

#### 1. 文档搜索功能
- 🔍 关键词搜索，实时匹配
- 🔍 正则表达式支持
- 🔍 大小写敏感选项
- 🔍 搜索结果计数和导航
- 🔍 智能填充选中文本
- 🔍 搜索结果列表

#### 2. 文档大纲导航
- 📋 自动提取所有标题
- 📋 层级缩进显示
- 📋 一键跳转章节
- 📋 当前位置高亮
- 📋 实时同步更新

#### 3. 工具栏按钮
- 新增搜索按钮（🔍）
- 新增大纲按钮（📋）
- 更直观的交互方式

### 🌟 体验优化

- 🌍 界面全面中文化
- ⌨️ 修复快捷键冲突（Cmd+Shift+B → Cmd+Shift+C）
- ✨ 搜索面板毛玻璃效果
- 📝 编辑器占位符添加快捷键提示
- 🎨 优化空状态显示和友好提示
- 🐛 修复搜索时焦点跳转问题

### ⚡️ 性能提升

- 清理冗余调试日志
- 优化应用启动速度
- 提升搜索响应速度
- 减少内存占用

---

## 📞 需要帮助？

### 遇到问题或有功能建议？

**立即添加微信：AIPMAndy**

（添加时请备注：MDskill）

### 我可以帮你：
- ✅ 解决安装和使用问题
- ✅ 提供功能使用指导
- ✅ 收集功能改进建议
- ✅ 定制专属主题
- ✅ 企业版功能咨询

**响应时间**：通常 1 小时内回复

---

## 💝 关于 MDskill

MDskill 是一款专为 Mac 用户打造的专业 Markdown 编辑器，致力于提供：
- ✨ 极致的编辑体验
- 🎨 精美的主题系统
- 🚀 高效的工作流程
- 💡 持续的功能更新

感谢你选择 MDskill！

---

<div align="center">

**MDskill - 让 Markdown 编辑更高效**

如有任何问题或建议，欢迎随时联系

**微信：AIPMAndy**

⭐️ 觉得好用？欢迎推荐给朋友！

</div>
